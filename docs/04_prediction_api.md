# Prediction API

## Overview

The FastAPI application in `src/mariscos_api/` exposes the trained XGBoost model as a REST endpoint. It is designed for local development and can be deployed to any ASGI-compatible server.

## Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/` | Root — returns service name and docs link |
| `GET` | `/health` | Health check — returns `{"status": "ok"}` |
| `POST` | `/predict` | Demand prediction for a product, branch, and date |

Interactive API documentation is auto-generated by FastAPI and available at:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## Running Locally

```bash
# Option 1 — via Makefile
make run-api

# Option 2 — via uv directly
uv run uvicorn mariscos_api.main:app --reload

# Option 3 — via the project entry point
uv run api
```

## Key Dependencies

| Package | Role |
|---|---|
| FastAPI | Web framework |
| Uvicorn | ASGI server |
| Pydantic | Request/response validation |
| MLflow | Model loading from experiment store |
| Joblib | Artifact deserialization |
| Pandas | Data transformation for inference |
| Scikit-learn | Preprocessing pipeline |

## Project Structure

```
src/
└── mariscos_api/
    ├── __init__.py   # Package declaration
    └── main.py       # FastAPI app and endpoint definitions
```
